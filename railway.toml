[build]
builder = "nixpacks"
buildCmd = "composer install && npm install && npm run build && php artisan optimize && php artisan config:cache"

[deploy]
startCommand = "php artisan deploy:serve --port $PORT"
restartPolicyMaxRetries = 5
